import {useEffect,useState} from 'react';
import {Boxes} from 'lucide-react';
import ResourceIcon from './ResourceIcon';
const items=[['foodstuffs','Foodstuffs','Food'],['timber','Timber','Bauxite'],['fibers','Fibers','Food'],['basic_metals','Basic Metals','Iron'],['energy','Energy','Oil'],['strategic_minerals','Strategic Minerals','Uranium'],['textiles','Textiles','Aluminum'],['processed_foods','Processed Foods','Food'],['construction_materials','Construction Materials','Steel'],['basic_goods','Basic Goods','Steel'],['consumer_goods','Consumer Goods','Aluminum'],['military_equipment','Military Equipment','Munitions'],['luxury_goods','Luxury Goods','Uranium']] as const;
export default function ResourceDock(){const[data,setData]=useState<any>();useEffect(()=>{const load=()=>Promise.all([fetch('/api/me',{credentials:'include'}).then(r=>r.json()),fetch('/api/strategy',{credentials:'include'}).then(r=>r.json())]).then(([me,strategy])=>setData({nation:me.nation,stockpiles:strategy.stockpiles||{}}));void load();const timer=setInterval(load,15000);return()=>clearInterval(timer)},[]);if(!data)return <div className="resource-dock loading">Loading national stockpiles…</div>;return <section className="resource-dock" aria-label="Resources on hand"><div className="dock-title"><Boxes size={15}/><span>ON HAND</span></div><div className="dock-item cash"><ResourceIcon type="Treasury"/><div><span>Treasury</span><b>¥{Number(data.nation.Treasury||0).toLocaleString()}</b></div></div>{items.map(([key,label,icon])=><div className="dock-item" key={key}><ResourceIcon type={icon}/><div><span>{label}</span><b>{Number(data.stockpiles[key]||0).toLocaleString()}</b></div></div>)}</section>}
