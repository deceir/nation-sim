import {useEffect,useState} from 'react';
import {Megaphone,Moon,Sun} from 'lucide-react';
import {applyTheme,storedTheme,type Theme} from './theme';
const save=async(theme:Theme)=>{applyTheme(theme);const r=await fetch('/api/user/settings',{method:'PATCH',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({theme})});if(!r.ok)throw Error('Theme preference could not be saved.')};
const responseData=async(r:Response)=>{const text=await r.text();if(!text)return{};try{return JSON.parse(text)}catch{throw Error(r.status===404?'The broadcast endpoint is not available yet. Redeploy the Diplomatia backend, then try again.':`Diplomatia returned an invalid response (HTTP ${r.status}).`)}};
export default function SettingsScreen(){const[theme,setTheme]=useState<Theme>(storedTheme()),[message,setMessage]=useState(''),[isDev,setIsDev]=useState(false);useEffect(()=>{void fetch('/api/me',{credentials:'include'}).then(r=>r.json()).then(x=>setIsDev(x.nation?.UserType==='DEV'))},[]);const choose=async(next:Theme)=>{setTheme(next);try{await save(next);setMessage('Theme preference saved to your account.')}catch(e){setMessage((e as Error).message)}};return <section className="settings-page"><div className="section-intro"><span className="eyebrow">ACCOUNT SETTINGS</span><h2>Appearance</h2><p>Theme preference is synchronized with your account.</p></div><div className="theme-grid"><button className={'theme-choice dark-preview '+(theme==='dark'?'selected':'')} onClick={()=>choose('dark')}><div className="theme-swatch"><Moon/><span/><span/></div><b>Dark</b></button><button className={'theme-choice light-preview '+(theme==='light'?'selected':'')} onClick={()=>choose('light')}><div className="theme-swatch"><Sun/><span/><span/></div><b>Light</b></button></div>{message&&<p className="settings-message">{message}</p>}{isDev&&<DevBroadcast/>}</section>}
function DevBroadcast(){const[title,setTitle]=useState(''),[body,setBody]=useState(''),[status,setStatus]=useState(''),[busy,setBusy]=useState(false);const send=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);setStatus('');try{const r=await fetch('/api/dev/notifications',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({title,message:body})}),x=await responseData(r);if(!r.ok)throw Error(x.error||`Broadcast failed (HTTP ${r.status}).`);setStatus(`Game notification sent to ${x.recipients} players.`);window.dispatchEvent(new Event('diplomatia:notifications'));setTitle('');setBody('')}catch(e){setStatus((e as Error).message)}finally{setBusy(false)}};return <form className="panel dev-broadcast" onSubmit={send}><Megaphone/><span className="eyebrow">DEVELOPER CONTROL</span><h2>Broadcast Game notification</h2><p>This sends one Game-category notification to every PLAYER and DEV nation. Bots are excluded.</p><label>Title<input value={title} minLength={3} maxLength={120} onChange={e=>setTitle(e.target.value)} required/></label><label>Message<textarea value={body} minLength={3} maxLength={1000} rows={5} onChange={e=>setBody(e.target.value)} required/></label><button className="primary" disabled={busy}>{busy?'Sending…':'Send to all players'}</button>{status&&<p className="settings-message">{status}</p>}</form>}
