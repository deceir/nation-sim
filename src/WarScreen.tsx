import {useEffect,useState} from 'react';
import {Activity,Clock3,Crosshair,Flag,MapPinned,Shield,ShieldCheck,Swords,Truck} from 'lucide-react';
import './war-system.css';

const api=async(path:string,options?:RequestInit)=>{const response=await fetch('/api'+path,{credentials:'include',headers:{'Content-Type':'application/json'},...options}),text=await response.text();let data:any={};try{data=text?JSON.parse(text):{}}catch{throw Error(`Invalid war response (${response.status}).`)}if(!response.ok)throw Error(data.error||'War action failed.');return data};
const units=['soldiers','tanks','ships','jets','drones'];
const names:Record<string,string>={soldiers:'Soldiers',tanks:'Tanks',ships:'Ships',jets:'Fighter Jets',drones:'Drones'};
const label=(value:string)=>value.replaceAll('_',' ').replace(/\b\w/g,letter=>letter.toUpperCase());
const when=(value:string)=>new Date(value).toLocaleString(undefined,{dateStyle:'medium',timeStyle:'short'});

export default function WarScreen({warID}:{warID?:string}){
 return warID?<WarDetail id={warID}/>:<WarOverview/>;
}

function WarOverview(){
 const initialTarget=new URLSearchParams(location.search).get('target')||'';
 const[data,setData]=useState<any>(),[military,setMilitary]=useState<any>(),[error,setError]=useState(''),[view,setView]=useState<'active'|'history'|'declare'>(initialTarget?'declare':'active');
 const[target,setTarget]=useState(initialTarget),[objective,setObjective]=useState('territorial_pressure'),[forces,setForces]=useState<Record<string,number>>({}),[review,setReview]=useState(false),[busy,setBusy]=useState(false);
 const load=()=>Promise.all([api('/wars'),api('/military')]).then(([wars,military])=>{setData(wars);setMilitary(military)}).catch(e=>setError(e.message));
 useEffect(()=>{void load()},[]);
 const submit=async()=>{setBusy(true);setError('');try{const result=await api('/wars',{method:'POST',body:JSON.stringify({defenderName:target.trim(),objective,forces})});location.href='/war/'+encodeURIComponent(result.warID)}catch(e){setError((e as Error).message);setReview(false)}finally{setBusy(false)}};
 if(!data||!military)return <section className="panel wide">Opening the strategic command room…</section>;
 const active=data.wars.filter((war:any)=>war.stage!=='ended'),history=data.wars.filter((war:any)=>war.stage==='ended'),list=view==='history'?history:active;
 return <div className="war-page"><section className="war-hero"><Crosshair/><div><span className="eyebrow">STRATEGIC COMMAND</span><h2>War Room</h2><p>Direct finite campaigns through six-hour strategic rounds. Distance delays deployment, supply affects combat power, and every war reaches a firm conclusion.</p></div></section>{error&&<p className="error notice">{error}</p>}<section className="war-status-strip"><div><Activity/><span>War exhaustion</span><b>{Number(data.status.warExhaustion||0).toFixed(1)}%</b></div><div><Swords/><span>Offensive capacity</span><b>{active.filter((w:any)=>w.isAttacker).length} / {data.rules.offensiveSlots}</b></div><div><Shield/><span>Defensive capacity</span><b>{active.filter((w:any)=>!w.isAttacker).length} / {data.rules.defensiveSlots}</b></div><div><Clock3/><span>Campaign limit</span><b>{data.rules.maximumRounds} rounds</b></div></section><nav className="war-tabs"><button className={view==='active'?'active':''} onClick={()=>setView('active')}>Active wars <span>{active.length}</span></button><button className={view==='history'?'active':''} onClick={()=>setView('history')}>War history</button><button className={view==='declare'?'active':''} onClick={()=>setView('declare')}>Declare war</button></nav>{view!=='declare'?<section className="war-list">{list.length?list.map((war:any)=><a className="war-card" href={`/war/${encodeURIComponent(war.id)}`} key={war.id}><div><span className={'war-stage '+war.stage}>{war.stage}</span><h3>{war.attackerName} <small>versus</small> {war.defenderName}</h3><p>{war.objectiveName}</p></div><div className="war-card-score"><span>{Number(war.attackerScore).toFixed(1)} — {Number(war.defenderScore).toFixed(1)}</span><b>{Number(war.attackerResolve).toFixed(0)}% · {Number(war.defenderResolve).toFixed(0)}% resolve</b></div><div className="war-card-meta"><span>Round {war.roundsResolved} / {data.rules.maximumRounds}</span><span>{Math.round(Number(war.distanceKm)).toLocaleString()} km · {label(war.routeType)}</span><span>{war.stage==='ended'?(war.outcome?label(war.outcome):'Concluded'):`Next round ${when(war.nextRoundAt)}`}</span></div></a>):<div className="panel war-empty"><ShieldCheck/><h3>{view==='history'?'No concluded wars':'No active wars'}</h3><p>{view==='history'?'Completed campaigns will remain available here as a permanent record.':'Your armed forces currently have no active commitments.'}</p></div>}</section>:<section className="panel war-declaration"><div className="war-section-heading"><div><span className="eyebrow">FORMAL DECLARATION</span><h2>Open a campaign</h2></div><Flag/></div><label>Defending nation<input value={target} onChange={event=>{setTarget(event.target.value);setReview(false)}} placeholder="Enter the exact nation name"/></label><div className="war-objectives">{Object.entries(data.objectives).map(([key,value]:any)=><button type="button" className={objective===key?'selected':''} onClick={()=>{setObjective(key);setReview(false)}} key={key}><b>{value.name}</b><span>{value.description}</span></button>)}</div><div className="deployment-editor"><h3>Initial force commitment</h3><p>Committed forces cannot be sold, decommissioned, or used in another war. The force arrives after the distance-based mobilization period.</p>{military.units.map((unit:any)=><label key={unit.key}><span><b>{unit.name}</b><small>{Number(unit.availableQuantity).toLocaleString()} available</small></span><input type="number" min="0" max={unit.availableQuantity} step="1" value={forces[unit.key]||''} onChange={event=>{setForces({...forces,[unit.key]:Math.max(0,Math.floor(Number(event.target.value))) });setReview(false)}}/></label>)}</div>{review?<div className="war-final-confirm"><b>Confirm declaration against {target || 'this nation'}?</b><p>Guardian status is revoked when the declaration succeeds. This starts a real campaign and locks the selected units into it.</p><button className="primary danger-action" disabled={busy||!target.trim()} onClick={()=>void submit()}>{busy?'Declaring war…':'Confirm declaration'}</button><button disabled={busy} onClick={()=>setReview(false)}>Go back</button></div>:<button className="primary" disabled={!target.trim()||Object.values(forces).reduce((sum,value)=>sum+Number(value||0),0)<1} onClick={()=>setReview(true)}>Review declaration</button>}</section>}</div>
}

function WarDetail({id}:{id:string}){
 const[data,setData]=useState<any>(),[error,setError]=useState(''),[operation,setOperation]=useState('hold'),[posture,setPosture]=useState('entrenched'),[forces,setForces]=useState<Record<string,number>>({}),[busy,setBusy]=useState(''),[confirmCapitulation,setConfirmCapitulation]=useState(false);
 const load=()=>api('/wars/'+encodeURIComponent(id)).then(setData).catch(e=>setError(e.message));
 useEffect(()=>{void load();const timer=setInterval(load,30000);return()=>clearInterval(timer)},[id]);
 const saveOrders=async()=>{setBusy('orders');setError('');try{await api(`/wars/${id}/orders`,{method:'PUT',body:JSON.stringify({operation,posture})});await load()}catch(e){setError((e as Error).message)}finally{setBusy('')}};
 const deploy=async()=>{setBusy('deploy');setError('');try{await api(`/wars/${id}/deploy`,{method:'POST',body:JSON.stringify({forces})});setForces({});await load()}catch(e){setError((e as Error).message)}finally{setBusy('')}};
 const capitulate=async()=>{setBusy('capitulate');setError('');try{await api(`/wars/${id}/capitulate`,{method:'POST'});await load();setConfirmCapitulation(false)}catch(e){setError((e as Error).message)}finally{setBusy('')}};
 if(!data)return <section className="panel wide">Loading the campaign record…{error&&<p className="error notice">{error}</p>}</section>;
 const mySide=data.isAttacker?'attacker':'defender',enemySide=data.isAttacker?'defender':'attacker',myForces=data.forces[mySide]||{},enemyForces=data.forces[enemySide]||{};
 return <div className="war-page war-detail"><a className="back" href="/wars">← War Room</a><section className="war-detail-hero"><div><span className={'war-stage '+data.stage}>{data.stage}</span><h2>{data.attackerName} <small>versus</small> {data.defenderName}</h2><p>{data.objectiveName} · {data.objectiveDescription}</p></div><div><b>Round {data.roundsResolved} / 20</b><span>{data.stage==='ended'?`${label(data.outcome||'concluded')} · ${label(data.endReason||'ended')}`:`Next resolution ${when(data.nextRoundAt)}`}</span></div></section>{error&&<p className="error notice">{error}</p>}<section className="war-balance"><WarSide name={data.attackerName} score={data.attackerScore} resolve={data.attackerResolve} readiness={data.attackerReadiness} organization={data.attackerOrganization} active={data.isAttacker}/><div className="war-versus"><Swords/><span>{Math.round(Number(data.distanceKm)).toLocaleString()} km</span><small>{label(data.routeType)} route · {data.mobilizationRounds} round mobilization</small></div><WarSide name={data.defenderName} score={data.defenderScore} resolve={data.defenderResolve} readiness={data.defenderReadiness} organization={data.defenderOrganization} active={!data.isAttacker}/></section><section className="war-force-comparison"><ForceTable title="Your committed forces" forces={myForces}/><ForceTable title="Opposing observed forces" forces={enemyForces}/></section>{data.stage!=='ended'&&<><section className="panel war-orders"><div className="war-section-heading"><div><span className="eyebrow">NEXT STRATEGIC ROUND</span><h2>Operational orders</h2></div><Crosshair/></div><div className="war-order-grid"><label>Operation<select value={operation} onChange={e=>setOperation(e.target.value)}>{Object.entries(data.operations).map(([key,value]:any)=><option value={key} key={key}>{value}</option>)}</select></label><label>Posture<select value={posture} onChange={e=>setPosture(e.target.value)}>{Object.entries(data.postures).map(([key,value]:any)=><option value={key} key={key}>{value}</option>)}</select></label></div><p>Orders can be revised until the round resolves. If no order is submitted, forces hold an entrenched posture.</p><button className="primary" disabled={busy!==''} onClick={()=>void saveOrders()}>{busy==='orders'?'Saving orders…':'Save round orders'}</button></section><section className="panel war-reinforcements"><div className="war-section-heading"><div><span className="eyebrow">FORCE COMMITMENT</span><h2>Deploy reinforcements</h2></div><Truck/></div><div className="reinforcement-inputs">{units.map(unit=><label key={unit}><span>{names[unit]}</span><input type="number" min="0" step="1" value={forces[unit]||''} onChange={e=>setForces({...forces,[unit]:Math.max(0,Math.floor(Number(e.target.value)))})}/></label>)}</div><p>Attacking reinforcements use the campaign’s distance delay. Home defenders arrive for the next strategic round.</p><button disabled={busy!==''||Object.values(forces).reduce((sum,value)=>sum+Number(value||0),0)<1} onClick={()=>void deploy()}>{busy==='deploy'?'Deploying…':'Commit reinforcements'}</button></section></>}<section className="war-reports"><div className="war-section-heading"><div><span className="eyebrow">AFTER-ACTION RECORD</span><h2>Strategic reports</h2></div><Activity/></div>{data.reports.length?data.reports.map((report:any)=><article className="war-report" key={report.round}><header><b>Round {report.round}</b><time>{when(report.resolvedAt)}</time></header><p>{report.summary}</p><div><span>{label(report.attackerOperation)} · {Number(report.attackerSupply*100).toFixed(0)}% supplied</span><span>{label(report.defenderOperation)} · {Number(report.defenderSupply*100).toFixed(0)}% supplied</span></div><small>Losses: {lossText(report.attackerLosses)} / {lossText(report.defenderLosses)}</small></article>):<div className="war-empty compact">No strategic rounds have resolved yet.</div>}</section>{data.stage!=='ended'&&<section className="war-capitulation">{confirmCapitulation?<><div><b>Confirm capitulation?</b><p>This immediately concedes the campaign and begins post-war reconstruction.</p></div><button className="danger" disabled={busy!==''} onClick={()=>void capitulate()}>{busy==='capitulate'?'Concluding war…':'Confirm capitulation'}</button><button disabled={busy!==''} onClick={()=>setConfirmCapitulation(false)}>Cancel</button></>:<button className="danger" onClick={()=>setConfirmCapitulation(true)}>Capitulate</button>}</section>}</div>
}

function WarSide({name,score,resolve,readiness,organization,active}:{name:string;score:number;resolve:number;readiness:number;organization:number;active:boolean}){return <article className={active?'my-side':''}><span>{active?'YOUR COMMAND':'BELLIGERENT'}</span><h3>{name}</h3><b>{Number(score).toFixed(1)} score</b><Meter label="Resolve" value={resolve}/><Meter label="Readiness" value={readiness}/><Meter label="Organization" value={organization}/></article>}
function Meter({label:meterLabel,value}:{label:string;value:number}){return <div className="war-meter"><span>{meterLabel}<b>{Number(value).toFixed(0)}%</b></span><i><em style={{width:`${Math.max(0,Math.min(100,Number(value)))}%`}}/></i></div>}
function ForceTable({title,forces}:{title:string;forces:Record<string,any>}){return <article><h3>{title}</h3>{units.map(unit=>{const force=forces[unit];return <div key={unit}><span>{names[unit]}</span><b>{Number(force?.remaining||0).toLocaleString()}</b><small>{force&&Number(force.arrivesRound)>0?`Arrives round ${force.arrivesRound}`:'In theater'}</small></div>})}</article>}
function lossText(losses:Record<string,number>){const parts=Object.entries(losses||{}).filter(([,value])=>Number(value)>0).map(([unit,value])=>`${Number(value).toLocaleString()} ${names[unit]}`);return parts.length?parts.join(', '):'none'}
