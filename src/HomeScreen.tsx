import {useEffect,useState} from 'react';
import {Building2,Globe2,ShoppingCart,Users} from 'lucide-react';

export default function HomeScreen(){const[data,setData]=useState<any>();useEffect(()=>{void fetch('/api/world/stats',{credentials:'include'}).then(r=>r.json()).then(setData)},[]);if(!data)return <section className="panel wide">Surveying the world…</section>;return <><section className="world-hero"><span className="eyebrow">DIPLOMATIA WORLD</span><h2>A living world shaped entirely by its nations.</h2><p>Track global growth, discover other governments, trade on the player-run exchange, and watch alliances and rivalries reshape the international order.</p></section><div className="world-stat-grid"><WorldStat icon={<Globe2/>} label="Registered nations" value={data.nations}/><WorldStat icon={<Building2/>} label="Developed cities" value={data.cities}/><WorldStat icon={<Users/>} label="World population" value={data.population}/><WorldStat icon={<ShoppingCart/>} label="Open market orders" value={data.openMarketOrders}/></div><section className="panel world-brief"><div><span className="eyebrow">CURRENT ACTIVITY</span><h2>{data.activePlayers} active {data.activePlayers===1?'player':'players'}</h2><p>Players who have taken an action during the last five minutes.</p></div><div className="world-pulse"><i/> LIVE WORLD</div></section></>}
function WorldStat({icon,label,value}:{icon:React.ReactNode,label:string,value:number}){return <section className="panel world-stat">{icon}<span>{label}</span><b>{Number(value).toLocaleString()}</b></section>}
